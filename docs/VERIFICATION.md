# 검증 기준표 — 소포수령증 매출집계

**검증 대상 목표:** 업로드된 원본 자료(PDF/CSV/Excel)가 누락·중복 없이 파싱되고,
올바른 환율로 환산되어, 매출집계 워크북과 신고서류(영세율첨부서류제출명세서·수출실적명세서)에
정확히 반영된다.

검증은 성격이 다른 세 축으로 돌려 상호 보완한다.

1. **회귀 축** (A-1, E-3): fixtures + expected로 자동 반복 — 코드 변경 때마다 돌리는 기본 안전망
2. **독립 대사 축** (A-2~A-5, C 전체): 생성 로직과 무관한 경로로 원본을 다시 세어보기
   (`sales-summary-verifier` 에이전트). 회귀 축이 못 잡는 "기대값 자체가 틀린" 경우를 잡음
3. **규칙 축** (B, D, E): 숫자가 우연히 맞아도 규칙(환율 기준일, 헤더 위치, 큐텐 예외)이
   어긋나면 잡아내는 층

---

## A. 정확성 — 숫자가 맞는가

| # | 검증 항목 | 합격 기준 | 검증 방법 | 근거 자료 |
|---|---|---|---|---|
| A-1 | 회귀 일치 | 실행 결과가 `expected/summary.json`의 건수·수량·외화·원화와 **완전 일치** | fixtures 환율을 `data/`에 복사 후 각 플랫폼 `input/`을 처리, 결과와 JSON 대조 | `samples/*/expected/summary.json` |
| A-2 | 원본 대사 | 워크북 데이터행 수·외화 합계 = 원본 PDF/CSV의 거래 수·합계 행 | 원본을 독립적으로 재파싱해 행 단위 대조 (`sales-summary-verifier` 에이전트) | `samples/*/input/` |
| A-3 | 외부 정답지 교차 | 고객 수기본·기존 산출물과 원화 합계 일치 | 쇼피파이=수기본 `총` 시트 `위하고` 값, 린코스=`매출집계_1~3월.xlsx`, Joom=PDF 합계 행 254.83 USD | `samples/*/reference/` |
| A-4 | 선언 합계 대조 | 파서의 `declared_total`(PDF 합계 행)과 거래 행 합산이 일치 (`total_mismatch=False`) | Joom 등 합계 행이 있는 자료에서 파서 출력 확인 | 파서 결과 dict |
| A-5 | 내부 정합 | 총집계 = 월별집계 합 = 통화별 시트 합 = 신고서류 합 (같은 워크북 안에서 4중 일치) | 생성된 xlsx의 시트 간 합계를 openpyxl로 크로스체크 | 생성 워크북 |

## B. 환율 — 환산이 맞는가

| # | 검증 항목 | 합격 기준 | 검증 방법 | 근거 자료 |
|---|---|---|---|---|
| B-1 | 기준일 규칙 | 플랫폼별 기준일 매트릭스 준수 (쇼피=거래일, 라자다Excel=deliveredDate, 라자다PDF=기간평균, Joom=발송일, 쇼피파이=Fulfilled at, 린코스=발행월 월평균, 큐텐=반기말 월평균) | 샘플 행 몇 개를 골라 적용 환율의 날짜/월을 수동 확인 | ARCHITECTURE.md §3 |
| B-2 | 100통화 정규화 | JPY·IDR·VND가 1통화 기준으로 환산 (예: 941.39/100엔 → 9.4139원/엔), 환율 시트 표시는 100통화 값 유지 | 환율(JPY) 시트 표시값 × 외화 ÷ 100 = 원화인지 검산 | `환율(<통화>)` 시트 |
| B-3 | 월평균 공식값만 사용 | SMBS 공식 월평균 없으면 자체 평균으로 대체하지 않고 `RuntimeError`로 중단 | 월평균 캐시를 비우고 실행해 중단되는지 확인 (`fx-rate-auditor`) | `data/monthly_exchange_rate_cache.csv` |
| B-4 | 휴일 ffill | 휴일 거래는 **직전 영업일** 환율 적용 | 주말 날짜 거래 행의 적용 환율이 금요일 값인지 확인 | 일별 환율 캐시 |
| B-5 | 큐텐 삼중 일치 | `_qoo10_reporting_month`가 세 곳(app.py 수집·excel_writer 조회·extra_docs 신고행)에서 같은 월 반환 — 10월 PDF도 환율은 12월 월평균, 선적일자는 그 달 말일 | qoo10 샘플 3개월 실행, 세 산출물의 환율 월 비교 | ARCHITECTURE.md §6 |

## C. 완전성 — 빠지거나 겹치지 않았는가

| # | 검증 항목 | 합격 기준 | 검증 방법 |
|---|---|---|---|
| C-1 | 누락 없음 | 원본 거래 수 = 시트 데이터행 수 + 의도적 제외 수 | 원본 재카운트 후 `skipped_unfulfilled`·`skipped_blank`와 합산 대조 |
| C-2 | 제외 사유 명시 | 제외된 행마다 사유가 분류됨 (미배송·빈 행 등), 사유 없는 소실 0건 | 쇼피파이 `row_flags`의 `counted=False` 행 전수 확인 |
| C-3 | 중복 없음 | 같은 tracking_no/order_id가 두 번 계상되지 않음 (특히 `merge_lazada_results` 병합 후) | 통화별 시트에서 추적번호 중복 검사 |
| C-4 | 다중 파일 병합 | TH처럼 기간이 쪼개진 파일(10~11월 + 12월)이 합산도 되고 이중계상도 안 됨 | shopee 9개 PDF 일괄 처리 후 국가별 합계 확인 |

## D. 구조 — 산출물 형식이 맞는가

| # | 검증 항목 | 합격 기준 | 검증 방법 |
|---|---|---|---|
| D-1 | 시트 구성 | 총집계/월별집계/통화별/플랫폼 원본/환율 시트가 모두 존재, `keep_sheets` 미등록으로 삭제된 시트 없음 | 생성 워크북 시트 목록 대조 |
| D-2 | 헤더 위치 | 통화 시트는 1~5행 요약·6행 헤더·7행 데이터, **JPY(큐텐) 시트만 4행 헤더** — 어긋나면 수출신고 프로그램 업로드 실패 | 각 통화 시트 헤더 행 위치 확인 |
| D-3 | 신고서류 필수 필드 | 수출신고번호/기타영세율건수/선적일자/통화/환율/외화/원화 컬럼 채워짐, `_company_name()` 제출자 정상 추출 | extra_docs 산출물 셀 검사 |
| D-4 | 원본 재현 | 쇼피파이 원본 시트가 `headers`+`rows`로 원본 CSV 전 행을 그대로 재현 | 원본 CSV와 셀 단위 비교 |

## E. 견고성 — 이상 상황에서 안전하게 실패하는가

| # | 검증 항목 | 합격 기준 | 검증 방법 |
|---|---|---|---|
| E-1 | 판별 정확도 | 각 샘플 파일이 올바른 플랫폼으로 자동 판별, 미지 형식은 수동 선택으로 유도 | 전 샘플 `detect_pdf_type()` 결과 확인, 무관한 PDF 1개 투입 |
| E-2 | 조용한 오답 금지 | 환율 미확보·합계 불일치 시 그럴듯한 값으로 계속 진행하지 않고 명시적 오류/경고 | B-3 시나리오 + `total_mismatch` 플래그 동작 확인 |
| E-3 | 재현성 | fixtures 환율 사용 시 외부 사이트 상태와 무관하게 같은 실행이 같은 워크북 숫자 산출 | 동일 입력 2회 실행 후 diff |

---

## 검증 실행 이력

| 날짜 | 범위 | 결과 | 비고 |
|---|---|---|---|
